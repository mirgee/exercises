pub fn roman_to_integer() -> {
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_roman_to_integer_0() {
        let expected = todo!();
        let actual = roman_to_integer();
        assert_eq!(expected, actual);
    }
}
