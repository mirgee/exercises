import { design_a_stack_with_increment_operation } from '../src'

test('t0', () => {
  const expected = null;
  const actual = design_a_stack_with_increment_operation()
  expect(actual).toEqual(expected)
})
